export function Premise() {
  return (
    <section className="py-12 border-t border-border">
      <h2 className="text-primary text-[0.85rem] font-medium mb-6 lowercase section-header">
        <code>{`// premise`}</code>
      </h2>
      <p className="mb-4">
        all computations run on <span className="text-text keyword keyword-1">perfect conceptual computers</span>.
      </p>
      <p className="mb-4"><span className="text-text keyword keyword-2">correct by construction</span>. the result is saved.</p>
      <p className="mb-4">one <span className="text-text keyword keyword-3">content addressing</span> scheme. the hash is the artifact.</p>
      <p className="mb-4">
        <span className="text-text keyword keyword-4">ca-derivations</span> and buck2 and bazel are supports for a coset. they can have the same cache keys.
      </p>
      <p>who container registry. what nix cache. what waste.</p>
    </section>
  )
}
